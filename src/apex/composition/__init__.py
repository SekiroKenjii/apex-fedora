"""Turning the repository into build inputs: source bundles, manifests and remote runs.

A recipe here is a plan over stages. Nothing in this package orders anything by hand; the
order falls out of what each stage reads and writes.
"""
