"""Verification: what a disposable guest is asked, and what its answer is worth.

A probe case names the guest unit to run and the environment the guest must be in; a fault
case will name a deliberate breakage and the refusal the product must make. Both are declared
one file each and read through a sealed registry, so adding one is adding a file. The answers
come back as documents the attestation context files as proof.
"""
