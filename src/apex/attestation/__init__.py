"""Minting and judging attestations.

Named for what it does rather than for what it holds: a directory called evidence is
refused by the repository guard, because the runtime evidence store must never enter Git.
"""
